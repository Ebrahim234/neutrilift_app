import 'package:flutter/material.dart';

class RestOrTrainCard extends StatelessWidget {
  const RestOrTrainCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
